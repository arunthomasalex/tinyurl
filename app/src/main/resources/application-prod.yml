server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://tiny_db:3306/tiny?createDatabaseIfNotExists=true&autoReconnect=true&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
logging:
  level:
    root: ERROR
    run.tinyurl: DEBUG
    org.hibernate: DEBUG
